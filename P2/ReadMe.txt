ReadMe for the Program 2

1. Evaluated the code
2. Wrote Sudo Code for each functionality
3. Implemented the Sudo Code to the program

# stack.hpp
-> Added header title [comment]
-> Added isEmpty() function that returns topIndex == -1 -> stack.cpp

# stack.cpp
-> Added error message if it exceeds the stack limit
-> Implemented push(), pop(), top(), size(), display() & isEmpty()
-> In other words, defined the functions

# balancing.cpp
-> Added ValidPairs Function to define what is valid pairs of brackets
-> Push all opening bracket to the stack
-> Implemented the algorithm here

# infix2postfix.cpp
-> defined operands
-> Implemented the algorithm

# postfixEval.cpp
-> Defined the function doTheMath to perform calculation
-> Implemented the algorithm
